const express = require('express');
const router = express.Router();

//const ContentController = require('./ContentController');

//router.get('/', ContentController.contentPage);

module.exports = router;